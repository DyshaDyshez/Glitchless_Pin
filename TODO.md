# TODO: Адаптивная верстка и мобильные улучшения

## 1) Диагностика и план (выполнено)
- Проанализированы `index.html`, `style.css`, `script.js`.

## 2) Исправить мобильные проблемы (выполнить)
1. Сделать на mobile: вместо 5 кнопок темы — одну кнопку "Тема" с выпадающим меню выбора.
2. Исправить перекрытие контента системной клавиатурой:
   - скорректировать `overflow/min-height` в CSS для `.main`/`.page`,
   - добавить минимальный JS-хук с `visualViewport`/`resize` для корректного положения и скролла к активному textarea/input.
3. Исправить вылет кнопок экспорта Excel/JSON:
   - адаптивная раскладка `sidebar__footer` (wrap/stack/scroll) и размеров `.btn`.
4. Исправить логотип на mobile: размеры/выравнивание `.sidebar__logo-img` и `.sidebar__brand`.

## 3) Улучшения для ПК
1. Сайдбар должен занимать всю высоту экрана всегда (100vh / flex-stretch),
   - убывает/увеличивается вместе с высотой окна.
2. Убедиться, что при переходах по страницам (`.page hidden`) высота/скролл не ломаются.

## 4) Проверка
- Проверить вручную:
  - mobile (iOS/Android emulation): темы, клавиатура, экспорт, логотип.
  - desktop: сайдбар всегда full-height.

